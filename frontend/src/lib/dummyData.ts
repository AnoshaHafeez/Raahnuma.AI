/**
 * Marketplace records now come from the backend catalogue.
 *
 * This empty export remains only as a compatibility shim for prototype imports.
 */
import type { GearProduct } from "@/types/gear";

export const dummyGearProducts: GearProduct[] = [];
